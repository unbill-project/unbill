---
core.desc: A first route through the Unbill design lake.
core.name: Introduction
core.category:
  - core.narrative
core.belongs:
  - unbill
---

Read `unbill` first to understand the product boundary.
Then read `design-principles`, `data-model`, `device-console-split`,
`bill-amendment`, `sync-behavior`, and `security-model`.

For repository structure, read `workspace-layout`.
For user-facing surfaces, read `ui-shared-model` before the specific frontend entries.
For persistence and CRDT behavior, read `unbill-storage`, `ledger-doc`, and the store entries.
For runtime deployment, read `unbill-device`, `asymmetric-channel`, `symmetric-channel`,
`unbill-daemon`, and `unbill-server`.

Development changes should start at `development-workflow`.
That entry carries the repository's design-first and test-first discipline.

---

> **Sirno generated links begin. Do not edit this section.**

- core.belongs (to):
  - [unbill](unbill.md)
- core.belongs (from): (none)

> **Sirno generated links end.**
