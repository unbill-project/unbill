name: Version Release

on:
  push:
    tags:
      - 'v*'

jobs:
  build:
    uses: ./.github/workflows/build.yml
    with:
      tag: ${{ github.ref_name }}
    permissions:
      contents: read
      packages: write
    secrets: inherit

  release:
    needs: build
    uses: ./.github/workflows/release.yml
    with:
      tag: ${{ github.ref_name }}
      prerelease: false
      aur_packages: '["unbill-cli-bin","unbill-tui-bin","unbill-bin","unbill-daemon-bin"]'
      homebrew_packages: '["unbill-cli","unbill-tui"]'
    permissions:
      contents: write
      actions: read
    secrets: inherit
