[package]
name = "unbill-tauri"
description = "Tauri backend shell for unbill desktop."
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
homepage.workspace = true

[lib]
name = "unbill_tauri_lib"
crate-type = ["staticlib", "cdylib", "rlib"]

[features]
# Enable in production builds (cargo tauri build / Nix).
# Do NOT add to [features] default — the Tauri CLI sets this itself during
# `tauri dev` / `tauri build`; activating it here by default would break dev.
custom-protocol = ["tauri/custom-protocol"]

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
unbill-core = { path = "../unbill-core", features = ["local"] }
unbill-store-fs = { path = "../unbill-store-fs" }
anyhow.workspace = true
serde.workspace = true
tracing.workspace = true
tauri = { version = "2", features = [] }
tauri-plugin-shell = "2"
rustls = { version = "0.23", features = ["ring"] }

[dev-dependencies]
unbill-store-memory = { path = "../unbill-store-memory" }
tokio.workspace = true
tracing-subscriber.workspace = true
serde_json.workspace = true
