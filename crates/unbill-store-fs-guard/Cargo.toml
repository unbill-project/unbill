[package]
name = "unbill-store-fs-guard"
description = "Generic OS file-lock–based LockableStore guard for any LedgerStore."
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
homepage.workspace = true

[dependencies]
unbill-storage = { path = "../unbill-storage" }
unbill-model = { path = "../unbill-model" }
async-trait.workspace = true
tokio.workspace = true

[dev-dependencies]
unbill-store-fs = { path = "../unbill-store-fs" }
tempfile = "3"
