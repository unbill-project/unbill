# net — Implementation

Thin re-export of `UnbillEndpoint`, `run_join_host`, `run_join_requester`, `run_sync_session`, and extension traits from `unbill-network`. All implementation detail lives in that crate.
